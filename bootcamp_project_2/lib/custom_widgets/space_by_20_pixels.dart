import 'package:flutter/material.dart';

class SpaceBy20Pixels extends StatelessWidget {
  const SpaceBy20Pixels({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 20,
    );
  }
}
