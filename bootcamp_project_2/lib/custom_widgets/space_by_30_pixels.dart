import 'package:flutter/material.dart';

class SpaceBy30Pixels extends StatelessWidget {
  const SpaceBy30Pixels({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 30,
    );
  }
}
