import 'package:flutter/material.dart';

class SpaceBy40Pixels extends StatelessWidget {
  const SpaceBy40Pixels({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 40,
    );
  }
}
