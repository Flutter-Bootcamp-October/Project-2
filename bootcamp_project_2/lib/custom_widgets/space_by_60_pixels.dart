import 'package:flutter/material.dart';

class SpaceBy60Pixels extends StatelessWidget {
  const SpaceBy60Pixels({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 60,
    );
  }
}
